---
    - name: Install nginx
      hosts: webserver
      become: true
      tasks:
        - name: Copy file with owner and permissions
          copy:
          src: ../certificates
          dest: /root
          owner: user-ansible
          group: user-ansible
          mode: '0644'