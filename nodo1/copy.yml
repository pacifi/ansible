---
    - name: Install nginx
      hosts: webserver
      become: true
    
    - synchronize:
      src: ../cerificates
      dest: /root/
      owner: user-ansible
      group: user-ansible
      mode: 0644