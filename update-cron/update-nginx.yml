- hosts: all

  tasks:
    - replace:
        path: /etc/hosts
        regexp: '/usr/share/nginx/html/html1'
        replace: '/usr/share/nginx/html/html2'

    - name: Restart Nginx, if not started
      when: "'nginx' in ansible_facts.packages"
      service:
        name: nginx
        state: restarted